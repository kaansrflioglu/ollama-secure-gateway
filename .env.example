# Port on which the API middleware will run
PORT=8181

# URL of the Ollama server (usually localhost:11434 if on the same server)
OLLAMA_HOST=http://127.0.0.1:11434

# Security API Key. The mobile app must send this key in the 'X-API-Key' header.
API_KEY=your_super_secret_api_key_here
